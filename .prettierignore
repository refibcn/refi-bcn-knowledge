dist/
dist-review-protected/
node_modules/
.astro/
package-lock.json
public/CNAME
public/CNAME.pending
# Minified deploy payload, not source: the atlas fetches this at runtime and
# pretty-printing it inflates the download 118 KB -> 210 KB.
public/geo/

# Generated store content, not prose: kb/<schema>/<slug>.md files are written
# by scripts/migrate-kb-to-md.mjs under a byte-fidelity contract (frontmatter +
# body must round-trip deepStrictEqual against the source entries). Prettier
# reformatting them would break that contract.
kb/

# prettier-plugin-astro cannot parse this file: its whole body is one JSX
# conditional whose branch contains a raw `<script is:inline>`, and the plugin
# tries to parse that script as an expression ("SyntaxError: Unexpected token,
# expected }"). The file is also deliberately hand-tuned for self-containment —
# every byte of KB-derived markup, CSS and JS has to stay inline so staticrypt
# encrypts all of it — so leaving it unformatted is the safer trade.
src/pages/review.astro
